(ns yaml-config-manager-ui.core-test
  (:require [clojure.test :refer [are deftest is testing]]
            [yaml-config-manager-ui.core :refer [screen]]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
